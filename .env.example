# ═══════════════════════════════════════════════════════════════
# Pueblo Delivery — Variables de entorno
# ═══════════════════════════════════════════════════════════════
# Copia este archivo como .env y rellena los valores:
#   cp .env.example .env

# ─── Supabase ─────────────────────────────────────────────────
# Obtener en: https://supabase.com/dashboard → Tu proyecto → Settings → API
# "Project URL" — URL pública del proyecto Supabase
NEXT_PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
# "anon public" key — clave pública para acceso con RLS desde el navegador
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# "service_role" key — clave privada con permisos elevados (omite RLS).
# ⚠️  NUNCA exponer en el cliente. Solo usar en el servidor (seed, servicios internos, operaciones admin).
SUPABASE_SERVICE_ROLE_KEY=""

# ─── CartoCiudad (gratuito, sin API key) ─────────────────────
CARTOCIUDAD_BASE_URL="https://www.cartociudad.es/geocoder/api/geocoder"

# ─── Cloudflare R2 (opcional en desarrollo) ──────────────────
CLOUDFLARE_R2_ACCESS_KEY_ID=""
CLOUDFLARE_R2_SECRET_ACCESS_KEY=""
CLOUDFLARE_R2_BUCKET_NAME="pueblo-delivery"
CLOUDFLARE_R2_ENDPOINT=""
CLOUDFLARE_R2_PUBLIC_URL=""

# ─── Email — Resend (opcional en desarrollo) ─────────────────
# En dev los emails se muestran en la consola del servidor
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Pueblo Delivery <noreply@pueblodelivery.es>"

# ─── Sentry (opcional en desarrollo) ─────────────────────────
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""
SENTRY_AUTH_TOKEN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
